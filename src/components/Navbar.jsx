import {React,useEffect,useState} from 'react'
import {link} from 'react-router-dom';

import {styles} from '../styles';
import {navlnks} from '../constants';
import {logo, menu, close} from '../assets';

const Navbar = () => {
  return (
    <div>Navbar</div>
  )
}

export default Navbar